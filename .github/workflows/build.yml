name: Build APK
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Extract project
        run: unzip -q app.zip -d extracted
      - name: List all files
        run: find extracted -maxdepth 4
